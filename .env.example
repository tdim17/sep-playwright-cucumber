# Local environment variables
# Copy this file, rename it to .env and fill in the real values
# The .env file is excluded from GitHub (.gitignore)
# For CI/CD — add these variables as GitHub Secrets (Settings → Secrets → Actions)

SEP_USERNAME=
SEP_PASSWORD=
CARD_NUMBER=
EXPIRATION_DATE=
CVC=
ZIP_CODE=
